/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  devIndicators: false,
  output: 'standalone',
  allowedDevOrigins: ['192.168.1.138'],
};

export default nextConfig;
